# Secrets
.env
**/.env
!.env.example
!**/.env.example

# MIDAS local state (embedded PostgreSQL connection info, pgdata path reference)
.midas/
**/.midas_pgdata/

# Legacy SQLite (no longer used)
*.db
